{
  "name": "blockchain",
  "version": "1.0.0",
"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "sync": "node scripts/sync-abi.js"
},
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.20.0"
  }
}